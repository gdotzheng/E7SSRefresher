# Local Claude config (personal, not shared)
.claude/settings.local.json

# Python
__pycache__/
*.pyc

# Generated captures / debug output (runtime artifacts, not source)
dryrun.png
gui_capture.png
debug_*.png
diag_*.png
tools/snapshot.png
tools/probe_*.png

# Keep templates/ — they are required for the bot to work
